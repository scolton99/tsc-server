var express = require('express');
var router = express.Router();

/* GET users listing. */
router.get('/', (req, res) => {
  
});

router.post('/', (req, res) => {
  console.log(res);
});

module.exports = router;
